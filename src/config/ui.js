// UI timings are milliseconds; dimensions are CSS pixels.
export const UI_CONFIG = Object.freeze({
  toastMs: 5000,
  sessionPollMs: 8000,
  loginPollMs: 1500,
  streamReconnectAttempts: 3,
  streamReconnectDelayMs: 1000,
  streamPreviewMinChars: 100,
  composerMinHeight: 240,
  composerInitialMaxHeight: 480,
  composerMaxRatio: 0.5,
  composerKeyboardStep: 20,
  downloadRevokeMs: 1000,
});
