<script setup>
import { t as __t } from "../../i18n/index.js";
</script>
<template>
  <article>
    <h1>
      {{ __t("계정은 그대로,") }}<br />{{ __t("AI는 자유롭게 연결합니다.") }}
    </h1>
    <p>
      {{
        __t(
          " CodeWith는 웹 서버에 접속하는 서비스입니다. 사용자가 별도 연결 프로그램을 실행하거나 프로그램 주소·연결 코드를 입력하지 않습니다. AI 인증·모델 설정·대화는 서버에 사용자별로 보관합니다. 기본 개인용·공유용은 별도의 OAuth 앱 등록이나 아이디·비밀번호 회원가입이 필요하지 않습니다. ",
        )
      }}
    </p>
    <h2>{{ __t("1. 로그인 후 AI 연결하기") }}</h2>
    <p>
      {{
        __t(
          " 먼저 CodeWith에 들어갑니다. 기본 personal 모드는 개인 계정으로 자동 진입합니다. 공유용과 서버 운영은 설정된 로그인 방식을 사용합니다. 워크스페이스를 열고 AI 대화창의 연결 설정에서 서비스 → 연결 방식 → 인증 순서로 진행하세요. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 인증을 마치면 사용 가능한 기본 모델을 자동 선택합니다. 같은 서비스를 다시 연결할 때 기존 모델이 여전히 사용 가능하면 선택을 유지합니다. 개인용 이름은 PERSONAL로 고정되며, 공유용·서버 운영에서는 계정 설정에서 표시 이름을 변경할 수 있습니다. ",
        )
      }}
    </p>
    <h3>{{ __t("채팅에서 외부 문서 확인하기") }}</h3>
    <p>
      {{
        __t(
          " 채팅의 AI 응답 설정에서 외부 문서 검색을 자동 또는 끄기로 선택합니다. 자동 모드에서는 Codex·Claude Code·API 연결 모두 필요한 경우 공개 웹을 검색하거나 전달한 링크를 읽습니다. 답변의 출처 링크로 원문을 확인하세요. 로그인·비공개 문서는 대상이 아니며 모델과 계정의 도구 지원 여부에 따라 사용할 수 있는 범위가 달라집니다. 검색에는 추가 사용량이 발생할 수 있고, Claude API는 조사 후 답변을 작성하는 추가 요청을 사용합니다. 이 설정은 일반 채팅에 적용되며 HTML 계획 생성·평가에는 적용되지 않습니다. ",
        )
      }}
    </p>
    <h2>{{ __t("2. 구독 로그인에서 나타날 수 있는 화면") }}</h2>
    <p>
      <b>Codex:</b
      >{{
        __t(
          " 서버와 브라우저를 같은 PC의 localhost/127.0.0.1로 사용하는 경우 공식 브라우저 콜백을 사용합니다. 도메인이나 네트워크 IP로 접속하면 원격 서버 환경으로 보고 공식 장치 코드 방식을 선택합니다. 공식 페이지에서 장치 코드를 입력하고 승인하세요. ChatGPT 보안 설정에서 Codex 장치 코드 인증을 허용해야 할 수 있습니다. 사용자가 로컬 SSH 포워딩 등 특수한 네트워크를 구성한 경우에는 배포 환경에 맞는 확인이 추가로 필요합니다. ",
        )
      }}
    </p>
    <p>
      <b>Claude Code:</b
      >{{
        __t(
          " 로그인을 누르면 새 탭에서 공식 인증을 진행합니다. Claude CLI가 코드 입력을 받을 준비가 되면 CodeWith에 “Claude 인증 코드 입력” 단계가 나타납니다. 공식 페이지가 발급한 코드 전체를 입력하고 “인증 완료”를 누르세요. 브라우저 콜백으로 인증이 완료되면 코드 입력 없이 진행합니다. CodeWith가 공급자 로그인 절차를 임의로 재구현하거나 비공식 OAuth 앱을 가장하지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 이 연결은 서버가 실행한 공식 CLI의 인증 결과를 사용합니다. 일반 웹사이트용 “Sign in with ChatGPT/Claude” OAuth 연동을 제공하는 것은 아닙니다. 서비스 정책·구독·조직 설정에 따라 인증이나 이용이 제한될 수 있습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 새 탭은 CodeWith 로그인 대기 페이지로 열리며 인증 주소가 준비되면 자동 이동합니다. 인증 완료 후 CodeWith는 탭 닫기를 요청하지만, 인증 사이트의 브라우저 격리 정책으로 탭 제어가 차단되면 해당 탭은 남을 수 있습니다. 원래 CodeWith 화면의 AI 연결 완료 처리는 이와 별도로 진행됩니다. ",
        )
      }}
    </p>
    <h2>{{ __t("3. CodeWith 계정과 AI 연결은 분리됩니다") }}</h2>
    <p>
      {{
        __t(
          " CodeWith 로그인은 운영 모드의 PERSONAL 고정 계정 시작, 로그인 키, 패스키, 이메일, 조직 계정으로 진행합니다. AI를 연결하지 않아도 워크스페이스를 만들고 명세를 작성할 수 있습니다. ",
        )
      }}<a href="/guide/deployment">{{ __t("운영 모드와 로그인 안내") }}</a
      >{{ __t("에 설정과 계정 복구 방법을 정리했습니다. ") }}
    </p>
    <p>
      {{
        __t(
          " 로그인한 계정에서 AI 연결을 추가합니다. Codex·Claude·API를 변경하거나 마지막 AI 연결을 해제해도 CodeWith의 사용자 ID와 워크스페이스는 유지됩니다. 동일 API 키나 AI 계정을 사용하더라도 서로 다른 CodeWith 사용자를 자동 병합하지 않습니다. 각 연결의 인증 파일, 모델 설정과 대화는 CodeWith 사용자별로 분리합니다. ",
        )
      }}
    </p>
    <h2>{{ __t("4. 데이터의 저장 위치와 접근 권한") }}</h2>
    <table>
      <thead>
        <tr>
          <th>{{ __t("정보") }}</th>
          <th>{{ __t("저장") }}</th>
          <th>{{ __t("접근") }}</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>{{ __t("명세·계획·자료·변경 이력") }}</td>
          <td>{{ __t("선택한 DB 또는 파일 저장소") }}</td>
          <td>{{ __t("승인된 워크스페이스 멤버") }}</td>
        </tr>
        <tr>
          <td>{{ __t("모델·effort·응답 설정") }}</td>
          <td>{{ __t("사용자 계정의 aiSettings") }}</td>
          <td>{{ __t("해당 사용자만") }}</td>
        </tr>
        <tr>
          <td>{{ __t("대화 원문·AI 제안") }}</td>
          <td>{{ __t("사용자·워크스페이스·명세별 대화") }}</td>
          <td>
            {{ __t("해당 사용자에게만, 현재 워크스페이스 접근 권한도 필요") }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("직접 입력한 API 키") }}</td>
          <td>{{ __t("AES-256-GCM 암호문") }}</td>
          <td>{{ __t("서버의 해당 사용자 요청 처리 과정") }}</td>
        </tr>
        <tr>
          <td>{{ __t("구독·Codex CLI 인증") }}</td>
          <td>{{ __t("서버의 사용자별 공식 CLI 저장소") }}</td>
          <td>
            {{
              __t("서버 프로세스. 공식 CLI 파일·OS 자격 증명 저장 방식 사용")
            }}
          </td>
        </tr>
      </tbody>
    </table>
    <p>
      {{
        __t(
          " AI 제안을 사용자가 명세에 반영하면 그 반영 내용과 작성자 기록은 팀에 공유합니다. 대화 원문이나 API 키를 JSON 공유 파일·HTML 문서, 멤버 정보에 포함하지 않습니다. 요청을 실제 수행할 때는 명세와 개인 대화가 선택한 AI 공급자에게 전달됩니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 브라우저에는 CodeWith의 HttpOnly 세션 쿠키와 패널 너비 같은 UI 설정만 새로 저장합니다. 예전 버전이 남긴 브라우저 저장 항목이나 외부 백업을 자동 삭제하지 않습니다. ",
        )
      }}
    </p>
    <h2>{{ __t("5. 운영자가 준비하는 것") }}</h2>
    <pre>
npm install
npm start</pre>
    <p>
      {{ __t(" 구독 연결에는 ")
      }}<b>{{ __t("CodeWith 서버가 실행되는 환경") }}</b
      >{{
        __t(
          "에 공식 Codex/Claude Code CLI를 설치해야 합니다. 각 사용자의 PC에는 CLI를 설치하지 않습니다. CLI가 PATH에 없으면 CODEWITH_CODEX_BIN 또는 CODEWITH_CLAUDE_BIN을 지정하세요. OpenAI/Claude API 직접 연결에는 CLI가 필요하지 않습니다. ",
        )
      }}
    </p>
    <pre>{{
      __t(
        " # HTTPS 도메인으로 팀에 제공할 때 CODEWITH_MODE=server CODEWITH_ORIGIN=https://codewith.example.com HOST=127.0.0.1 PORT=4310",
      )
    }}</pre>
    <p>
      {{
        __t(
          " 역방향 프록시에서 HTTPS를 제공하고 Host 헤더를 유지하세요. 일반 네트워크에 서비스할 때는 전송 중 인증 정보도 보호해야 합니다. 개발용 HTTP loopback과 모든 브라우저·운영체제·네트워크 조합의 동작을 동일하게 보장하지는 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 기본 데이터 폴더는 codewith/.codewith입니다. API 키의 암호화 키인 ",
        )
      }}<code>ai-vault.key</code
      >{{
        __t(
          "는 DB와 별도 파일로 생성하며 파일 권한을 0600으로 설정합니다. API 암호문 복구에 필요하므로 백업에서 빠뜨리지 마세요. 파일 시스템 권한과 서버 관리자 접근은 운영 환경에서도 관리해야 합니다. ",
        )
      }}
    </p>
    <p>
      {{ __t(" 공식 CLI 인증은 ") }}<code>ai/codex/connection_…</code>,
      <code>ai/claude/connection_…</code
      >{{
        __t(
          "에 구분해 저장합니다. Codex CLI를 경유하는 API 인증을 별도로 사용하는 경우 해당 CLI 인증 저장소에도 키가 있을 수 있습니다. 기본 화면의 API 연결은 CLI를 경유하지 않습니다. 모든 CLI 파일을 CodeWith의 AES 암호문으로 다시 포장하는 구현은 아니며 CLI가 정한 저장 방식과 OS 권한을 따릅니다. 운영자는 DB, ai-vault.key, ai 폴더를 함께 백업해야 합니다. 복구할 때는 서버를 중지하고 일관된 시점의 백업을 복원하세요. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 현재 단일 서버 프로세스 기준입니다. 취소·만료된 인증 시도는 정리하며 서버 재시작 시 사용자는 완료되지 않은 인증을 다시 시작합니다. 대화는 서버 저장소에 남아 재시작 후에도 조회됩니다. ",
        )
      }}
    </p>
    <h2>{{ __t("6. 이전 버전 데이터") }}</h2>
    <p>
      {{
        __t(
          " 기존 계정·워크스페이스·대화는 보존됩니다. 이전 AI 로그인 계정으로 다시 접근하려면 운영 모드에 맞는 CodeWith 로그인 수단을 추가하세요. personal은 PERSONAL 작업 프로필로 바로 열립니다. 기존에 사용하던 개인 프로필의 데이터는 유지합니다. 공유용·서버 운영에서 이전 계정에 접근하려면 운영자가 해당 계정의 복구 키를 발급할 수 있습니다. ",
        )
      }}<a href="/guide/deployment#recovery">{{
        __t("기존 계정 키 발급 방법")
      }}</a
      >{{ __t("을 확인하세요. ") }}
    </p>
    <h2>{{ __t("검증 범위") }}</h2>
    <p>
      {{
        __t(
          " 자동 테스트는 공급자 응답을 대체한 테스트 연결로 계정 재사용, 사용자별 권한, 대화·설정 저장, 비밀정보 비노출, 인증 실패를 검증합니다. 실제 구독 계정의 승인과 유료 모델 응답은 운영 환경에서 사용자가 연결해 확인해야 합니다. ",
        )
      }}
    </p>
    <p>
      <a href="https://developers.openai.com/codex/app-server"
        >Codex App Server</a
      >
      ·
      <a href="https://code.claude.com/docs/en/authentication">{{
        __t("Claude Code 공식 인증")
      }}</a>
      · <a href="/guide">{{ __t("CodeWith 전체 사용 안내") }}</a>
    </p>
  </article>
</template>
