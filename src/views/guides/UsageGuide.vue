<script setup>
import { t as __t } from "../../i18n/index.js";
</script>
<template>
  <article>
    <p class="tag">{{ __t("CODEWITH 0.2 · 사용 안내") }}</p>
    <h1>{{ __t("명세는 함께, AI 연결은 각자.") }}</h1>
    <p>
      {{
        __t(
          " CodeWith는 프로젝트의 공통 기준과 개발 단위의 명세를 HTML 화면에서 작성하는 웹 앱입니다. 사용자는 직접 요구사항과 계획을 편집할 수 있고, 필요할 때 자신의 LLM 계정을 연결합니다. 개인용·공유용·서버 운영 모드에 따라 접속 범위와 로그인 화면이 달라지며 명세 데이터는 같은 구조를 사용합니다. ",
        )
      }}
    </p>
    <nav>
      <a href="#run">{{ __t("설치·접속") }}</a
      ><a href="#accounts">{{ __t("계정 분리") }}</a
      ><a href="#workflow">{{ __t("명세 작성") }}</a
      ><a href="#history">{{ __t("변경 이력") }}</a
      ><a href="#ai">{{ __t("AI 연결") }}</a
      ><a href="#format">{{ __t("JSON 작성법") }}</a
      ><a href="#materials">{{ __t("목업·도표 자료") }}</a
      ><a href="#storage">{{ __t("DB 연결") }}</a
      ><a href="#operations">{{ __t("운영·제한") }}</a>
    </nav>
    <h2 id="run">{{ __t("1. 설치와 접속 형태") }}</h2>
    <p>
      {{
        __t(
          " Node.js 22.13 이상이 필요합니다. 먼저 npm install을 실행하세요. 프로젝트의 ",
        )
      }}<code>codewith</code>{{ __t(" 폴더에서 다음 명령을 실행하세요. ") }}
    </p>
    <pre>npm start</pre>
    <p>
      {{ __t(" 기본 주소는 ")
      }}<a href="http://localhost:4310">http://localhost:4310</a
      >{{
        __t(
          "입니다. 기본 개인용 모드에서는 워크스페이스 목록으로 자동 진입합니다. 사용 안내는 앱의 도움말에서 새 탭으로 열 수 있으며, 로그인 없이 읽을 수 있습니다. 안내를 포함한 앱 화면은 실행 중인 서버를 통해 접속합니다. ",
        )
      }}
    </p>
    <table>
      <thead>
        <tr>
          <th>{{ __t("사용 형태") }}</th>
          <th>{{ __t("실행 및 접속") }}</th>
          <th>{{ __t("데이터 위치") }}</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>{{ __t("각자 로컬에서 독립 사용") }}</td>
          <td>
            {{ __t(" 각 PC에서 ") }}<code>npm start</code>{{ __t(", 각자의 ")
            }}<code>localhost:4310</code>
          </td>
          <td>
            {{
              __t("각 PC에 독립 저장. 자동 동기화하지 않으며 JSON/HTML로 교환")
            }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("한 사람의 PC에 팀 접속") }}</td>
          <td>
            {{ __t(" 호스트에서 ")
            }}<code>npm start -- --mode shared --host 0.0.0.0 --port 4310</code
            >{{ __t(". 팀원은 ")
            }}<code>{{ __t("http://호스트의_LAN_IP:4310") }}</code>
          </td>
          <td>
            {{ __t("호스트 PC에 공동 저장. 호스트가 실행 중이어야 접속 가능") }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("공용 서버에 설치") }}</td>
          <td>
            {{ __t("같은 npm 앱 실행 후 도메인과 HTTPS 역방향 프록시 연결") }}
          </td>
          <td>{{ __t("서버의 영속 데이터 폴더에 공동 저장") }}</td>
        </tr>
      </tbody>
    </table>
    <p>
      <code>0.0.0.0</code
      >{{
        __t(
          "은 서버가 요청을 받을 인터페이스 설정입니다. 브라우저에 입력할 공유 주소는 PC 또는 서버의 실제 IP/도메인입니다. LAN 접근에는 해당 포트에 대한 운영체제 방화벽 설정이 필요할 수 있습니다. WSL에서 실행하면 Windows↔WSL 네트워크 설정에 따라 LAN 포트 전달도 필요할 수 있습니다. ",
        )
      }}
    </p>
    <h3>{{ __t("도메인으로 운영할 때") }}</h3>
    <pre>{{
      __t(
        " # Linux/macOS 예시 CODEWITH_MODE=server \\ CODEWITH_ORIGIN=https://codewith.example.com \\ CODEWITH_DATA_DIR=/srv/codewith-data \\ npm start -- --host 127.0.0.1 --port 4310",
      )
    }}</pre>
    <p>
      <code>codewith.example.com</code
      >{{
        __t(
          "은 본인 도메인으로 바꿉니다. 역방향 프록시에서 HTTPS를 종료하고 CodeWith로 전달할 때 원래 ",
        )
      }}<code>Host</code>{{ __t(" 헤더를 유지하세요. ")
      }}<code>CODEWITH_ORIGIN</code
      >{{
        __t(
          "은 외부에서 쓰는 정확한 출처이며 경로나 마지막 슬래시를 붙이지 않습니다. 이를 설정하면 도메인 요청을 허용하고 로그인 쿠키에 ",
        )
      }}<code>Secure</code>{{ __t("를 사용합니다. ") }}
    </p>
    <pre>{{
      __t(
        " # nginx의 해당 HTTPS server 블록 안 location 예시 location / { proxy_pass http://127.0.0.1:4310; proxy_set_header Host $http_host; proxy_http_version 1.1; proxy_buffering off; proxy_read_timeout 660s; client_max_body_size 12m; }",
      )
    }}</pre>
    <p>
      {{
        __t(
          " HTTP로 운영하는 LAN에서도 로그인 정보와 LLM 연결을 다룹니다. 신뢰할 수 없는 네트워크나 인터넷에 공개할 때는 HTTPS를 구성하세요. 위 예시는 도메인·인증서·방화벽을 자동 설치하지 않습니다. ",
        )
      }}
    </p>
    <h3>Windows PowerShell</h3>
    <pre>{{
      __t(
        ' # 로컬 또는 LAN npm start -- --mode shared --host 0.0.0.0 --port 4310 # 도메인으로 운영하는 서버의 환경변수 예시 $env:CODEWITH_MODE = "server" $env:CODEWITH_ORIGIN = "https://codewith.example.com" $env:CODEWITH_DATA_DIR = "D:\\CodeWithData" npm start -- --host 127.0.0.1 --port 4310',
      )
    }}</pre>
    <h2 id="accounts">{{ __t("2. CodeWith 계정과 개인 AI 연결") }}</h2>
    <p>
      {{
        __t(
          " 기본 personal은 소개 화면의 PERSONAL 계정 카드를 눌러 진입, shared는 초대와 개인 로그인 키, server는 설정된 키·패스키·이메일·조직 계정으로 로그인합니다. 별도 아이디·비밀번호 가입 화면은 없습니다. 자세한 설정과 계정 복구는 ",
        )
      }}<a href="/guide/deployment">{{ __t("운영 모드 안내") }}</a
      >{{ __t("에 설명되어 있습니다. ") }}
    </p>
    <p>
      {{
        __t(
          " AI는 CodeWith 로그인 후 원하는 서비스를 연결합니다. 서비스를 변경하거나 모두 해제해도 사용자 ID와 명세 작성 이력은 유지됩니다. 명세·계획·자료는 승인된 워크스페이스 멤버와 공유하고, AI 연결·모델 설정·대화는 서버에 사용자별로 저장합니다. 워크스페이스 소유자도 다른 사용자의 개인 대화와 인증 정보는 조회할 수 없습니다. ",
        )
      }}
    </p>
    <h2 id="common-settings">{{ __t("공통 설정과 스킬 사용하기") }}</h2>
    <p>
      {{
        __t(
          " 좌측의 CodeWith 로고는 전체 워크스페이스 목록으로 돌아갑니다. 그 아래 현재 워크스페이스 이름은 목록을 펼치는 선택기이며, 참여 중인 다른 워크스페이스로 전환할 수 있습니다. 기본 설정 메뉴는 현재 워크스페이스의 이름·목적·공유 범위·권한·멤버 수를 보여 줍니다. ",
        )
      }}
    </p>
    <p>
      {{ __t(" 워크스페이스 메인 우상단의 ") }}<b>{{ __t("편집") }}</b
      >{{
        __t(
          "에서 이름과 목적을 수정하거나 워크스페이스를 삭제할 수 있습니다. 전체 목록 카드에도 동일한 편집 버튼이 있습니다. 슈퍼관리자가 편집하며 저장하면 변경 이력에 커밋이 남습니다. 명세는 왼쪽 명세 메뉴의 추가 버튼으로 만듭니다. ",
        )
      }}
    </p>
    <h3>{{ __t("개인 공통 설정") }}</h3>
    <p>
      {{
        __t(
          " 홈 상단의 사용자 이름 또는 워크스페이스 왼쪽 아래 PERSONAL 프로필을 클릭해 ",
        )
      }}<b>{{ __t("개인 설정 → 스킬") }}</b
      >{{
        __t(
          "에서 자신의 AI 응답 선호를 작성합니다. 예를 들어 “한국어로 설명하고, 답을 주기 전에 내가 생각할 질문을 해 주세요”라고 작성할 수 있습니다. 계정별로 서버에 저장되어 모든 워크스페이스의 본인 대화에 적용됩니다. 팀원에게 공개되거나 워크스페이스 HTML·JSON에 포함되지 않지만, 답변 생성을 위해 본인이 연결한 AI 서비스에는 전달됩니다. ",
        )
      }}
    </p>
    <h3>{{ __t("워크스페이스 공통 설정") }}</h3>
    <p>
      {{ __t(" 왼쪽 ") }}<b>{{ __t("공통 설정 · 스킬 → 공통 설정 편집") }}</b
      >{{
        __t(
          "에서 팀이 지킬 AI 지침, 프로젝트 목적, 설계·개발 원칙, 공통 제약을 작성합니다. 이 내용은 모든 명세 대화에 전달되고 멤버와 공유되며 변경 이력과 내보내기에 포함됩니다. 개인 공통 설정은 기본 선호이고, 충돌 시 워크스페이스 공통 설정을 우선합니다. 스킬은 그 기준 안에서 필요한 절차를 추가합니다. 이는 AI에 제공하는 지침이지 프로그램으로 강제되는 코드 검증 규칙은 아닙니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 이 공통 설정은 CLAUDE.md·AGENTS.md와 같은 역할의 지침이지만, 해당 파일을 자동 생성하거나 IDE에 설치하지는 않습니다. 지금은 CodeWith의 명세 AI에 적용됩니다. ",
        )
      }}
    </p>
    <h3>{{ __t("스킬") }}</h3>
    <p>
      {{
        __t(
          " 스킬에는 호출 이름, 이름, 설명, 선택 방식, 지침, 활성화 여부를 작성합니다. 별도 규칙 분류나 명세·코드 적용 대상 필드는 없습니다. ",
        )
      }}<b>{{ __t("항상 적용") }}</b
      >{{ __t("은 매 대화에 포함하며, ") }}<b>{{ __t("자동 선택") }}</b
      >{{
        __t(
          "은 요청 내용 또는 명세 제목에 등록 키워드가 포함될 때 선택합니다. 현재 자동 선택은 LLM의 자율 판단이 아닌 키워드 일치입니다. ",
        )
      }}<b>{{ __t("직접 호출") }}</b
      >{{
        __t(
          "은 채팅에 /java-review 또는 $java-review를 입력하는 방식입니다. 자동 선택 키워드는 자동 선택을 고른 경우에만 입력합니다. ",
        )
      }}
    </p>
    <pre>{{
      __t(
        ' { "instructions": "완료 기준은 정상·예외·경계 상황을 구분하여 작성한다.", "purpose": "사용자가 스스로 구현할 수 있도록 요구사항과 계획을 합의한다.", "principles": ["불확실한 조건은 열린 질문으로 남긴다."], "constraints": ["Java 21"], "skills": [{ "id": "java-review", "name": "Java 검토", "description": "문자열 비교와 예외 조건을 검토한다.", "content": "equals와 ==의 차이를 설명하고 반례를 요청한다.", "trigger": "manual", "keywords": [], "enabled": true }] }',
      )
    }}</pre>
    <p>
      {{ __t(" 위 객체는 교환 파일의 ") }}<code>document.projectSpec</code
      >{{
        __t(
          "에 들어갑니다. 개인 공통 설정은 포함하지 않습니다. 이전 파일의 rules는 내용·선택 방식·활성화 상태를 유지한 스킬로 변환하고 scope 필드는 제거합니다. 한 워크스페이스의 스킬은 최대 100개이며, 공통 지침과 각 스킬 지침은 20,000자 이하입니다. ",
        )
      }}
    </p>
    <h3>{{ __t("블록형 에디터") }}</h3>
    <p>
      {{
        __t(
          " 문서와 AI 채팅 모두 작성 중인 화면에서 서식이 바로 보이는 에디터를 사용합니다. 별도의 Markdown 입력·미리보기 전환은 없습니다. 텍스트를 선택해 굵게·기울임·인라인 코드·링크를 적용하고, 블록 메뉴에서 제목·목록·번호 목록·인용·코드 블록·구분선을 선택하세요. 상단 도구 막대와 왼쪽 + 버튼 없이 작성합니다. 빈 문단에서 /를 입력하면 블록 목록이 열립니다. 방향키로 고르고 Enter로 적용하며 Escape로 닫을 수 있습니다. 목록에서는 Tab·Shift+Tab으로 단계도 바꿀 수 있습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 문서의 Enter는 새 문단이나 목록 항목을 만들고, 채팅의 Enter는 전송·Shift+Enter는 줄바꿈입니다. 한글 조합 중 Enter는 전송하지 않습니다. Ctrl/Cmd+B·I로 서식, Ctrl/Cmd+K로 링크, Ctrl/Cmd+Z로 실행 취소, Ctrl/Cmd+Shift+Z로 다시 실행합니다. Ctrl/Cmd+Alt+1~3은 제목, Ctrl/Cmd+Alt+0은 본문, Ctrl/Cmd+Shift+7·8은 번호·글머리 목록입니다. 링크 주소는 편집 화면 안에서 입력합니다. 채팅에서는 $ 다음에 스킬 식별번호를 입력하면 활성화된 스킬을 검색합니다. 방향키로 고르고 Tab·Enter로 삽입한 뒤 내용을 입력해 전송합니다. Esc는 후보 목록만 닫습니다. 채팅과 일반 문서의 블록 메뉴는 빈 문단에서 /로 엽니다. 예를 들어 $java-review는 스킬 호출, /code는 코드 블록 선택입니다. 코드 블록 안에서는 스킬 자동완성이 뜨지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 저장과 HTML 문서·JSON 공유 파일은 기존 문자열 형식을 유지하며 에디터가 내부적으로 변환합니다. 완료 기준은 항목을 하나씩 추가하는 단순 입력으로 유지합니다. 이름·식별번호, 원칙·제약 같은 항목 목록과 JSON·표·그래프 데이터, 원본 코드 입력도 블록 에디터로 변환하지 않습니다. 요구사항 자료 첨부는 기존 자료 추가 기능을 이용합니다. 이 버전은 텍스트 블록 편집을 제공하며 Notion 전체 기능을 복제하지는 않습니다. ",
        )
      }}
    </p>
    <p>
      {{ __t(" 편집기는 ")
      }}<a
        href="https://prosemirror.net/examples/markdown/"
        target="_blank"
        rel="noopener"
        >{{ __t("ProseMirror의 문서 모델과 Markdown 변환 방식") }}</a
      >{{
        __t(
          "을 바탕으로 구성했습니다. 모든 편집 코드는 앱에서 제공하고 외부 편집 서비스에 입력 내용을 보내지 않습니다. 개발 시 ",
        )
      }}<code>npm run build:editor</code
      >{{ __t("로 브라우저 번들을 다시 생성할 수 있습니다. ") }}
    </p>
    <h3>{{ __t("워크스페이스 대화와 명세 대화") }}</h3>
    <p>
      {{
        __t(
          " 명세가 없어도 워크스페이스 우상단 말풍선 버튼을 열어 AI와 대화할 수 있습니다. 기본 설정·공통 설정·전체 요구사항 화면에서는 프로젝트 전체의 목적과 명세를 문맥으로 전달합니다. 개별 명세를 열면 그 명세의 요구사항·계획을 중심으로 대화합니다. 현재 대상은 대화창 상단에 표시되며, 워크스페이스 전체 대화와 각 명세 대화는 본인 계정 안에서 별도로 보관됩니다. 공통 설정과 스킬은 두 대화 모두에 적용됩니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 워크스페이스 전체 대화의 변경 제안은 답변에서 논의하며, 특정 명세에 반영하려면 해당 명세를 선택하세요. 선택하지 않은 명세에 요구사항을 자동으로 추가하지 않습니다. ",
        )
      }}
    </p>
    <h2 id="workflow">{{ __t("3. 요구사항 → HTML 계획 → 최종 승인") }}</h2>
    <ol>
      <li>
        <b>{{ __t("공통 설정 · 스킬:") }}</b
        >{{ __t(" 프로젝트 목적·원칙·제약을 정합니다. 새 워크스페이스에는 ")
        }}<code>codewith-html-plan</code>{{ __t("과 ")
        }}<code>codewith-plan-ui</code
        >{{
          __t(
            " 스킬이 기본 등록됩니다. 기존 워크스페이스에도 공통 UI 스킬을 추가하며, 같은 식별번호의 사용자 스킬은 덮어쓰지 않습니다. 스킬 편집에서 구조·설명 깊이·검토 절차를 직접 바꿀 수 있습니다. 기존 워크스페이스는 계획 탭의 ‘기본 스킬 추가’로 등록합니다. 미등록 상태에서는 기본 구조를 사용합니다. ",
          )
        }}
      </li>
      <li>
        <b>{{ __t("요구사항:") }}</b
        >{{
          __t(
            " 각 명세의 요구사항과 완료 기준을 작성합니다. 새 요구사항 편집기는 기존 목록의 아래에 나타나며, 기존 항목 편집은 해당 카드 바로 아래에서 진행합니다. 삭제 확인은 식별번호·제목이 표시되는 모달로 열리며 배경을 흐리게 처리합니다. 취소하면 기존 항목을 유지합니다. 완료 기준은 ID와 한 항목씩 작성하는 입력칸을 유지합니다. ",
          )
        }}
      </li>
      <li>
        <b>{{ __t("첫 계획 생성:") }}</b
        >{{
          __t(
            " AI 연결을 완료한 뒤 계획 탭의 ‘AI로 계획 세우기’를 누릅니다. 기본은 단일 작성이며, 계획 실행 설정에서 검토 추가·후보 비교와 역할별 모델을 선택할 수 있습니다. AI가 작성한 요구사항별 본문을 CodeWith의 고정 UI 템플릿에 넣어 탭형 HTML 계획서로 조합하고 서버에 v1으로 저장합니다. 명령이나 실제 코드는 실행하지 않습니다. ",
          )
        }}
      </li>
      <li>
        <b>{{ __t("검토와 수정:") }}</b
        >{{
          __t(
            " 버전 목록에서 기준 버전을 고르고 ‘AI로 수정 요청’으로 변경할 내용을 설명합니다. 요청이 완료되면 기준 버전을 부모로 기록한 새 버전이 생성됩니다. ‘새 탭에서 열기 · 편집’에서는 계획 본문을 직접 편집하고 단계별 승인·수정 요청 또는 상단 전체 승인을 진행합니다. AI 수정 요청에는 저장 전 편집 내용 전체와 이전 평가 지적이 전달됩니다. 단계 승인은 전체 계획의 최종 승인과 구분됩니다. ",
          )
        }}
      </li>
      <li>
        <b>{{ __t("최종 승인:") }}</b
        >{{
          __t(
            " 검토를 마친 버전에서 ‘검토 후 이 버전 승인’을 누르고 확인 모달에서 승인합니다. v3이 있어도 v1을 선택할 수 있습니다. 새로운 버전 생성은 최종 선택을 변경하지 않으며, 승인 해제도 가능합니다. 요구사항·공통 설정·전달된 코드가 바뀌면 기존 계획에 재검토 표시가 생깁니다. 변경된 기준을 검토하고 새 버전으로 저장한 뒤 승인합니다. ",
          )
        }}
      </li>
    </ol>
    <h3>{{ __t("명세 기반 계획 개선") }}</h3>
    <p>
      {{
        __t(
          " 계획을 작성한 뒤 독립 평가하고, 부족한 완료 기준과 평가 지적을 보완합니다. 필수 기준을 모두 충족하고 차단 문제가 없으며 품질 점수가 설정한 목표 점수 이상이면 자동 개선을 멈추고 사람 승인을 기다립니다. 실행 설정에서 호출·시간 예산을 지정합니다. 2회 연속 진전이 없으면 접근을 바꾸고, 대안 시도도 진전이 없으면 중단합니다. 예산 소진이나 정체는 완료가 아니며, 최선의 평가본에서 이어갈 수 있습니다. 후보 비교와 모델 선택은 실행 설정에 있습니다. 평가 기준은 계획 탭의 평가 기준 → 워크스페이스 평가 기준 → 시스템 기본값 순으로 상속합니다. 항목명·판단 설명·배점 합계 100점과 목표 점수 1~100점을 설정할 수 있습니다(기본 목표 90점). 기본 평가 항목은 5개이며 추가·삭제할 수 있습니다. 각 항목의 배점은 1점 이상, 전체 합계는 100점이어야 합니다. 명세의 계획 전체가 목표를 충족해야 하며, 기존 버전의 점수와 당시 기준은 보존됩니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 계획 화면에서 기준별 판정과 근거, 현재 점수와 개선 이력을 확인합니다. 점수는 계획의 설계·검증 방법에 대한 평가이며 실제 테스트 결과나 진행률이 아닙니다. 한도에 도달하면 마지막 정상 평가본을 저장하며 ‘이어서 개선’으로 계속할 수 있습니다. 설정에서 다음 실행의 한도를 조정할 수 있습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 직접 편집한 버전은 이전 평가를 물려받지 않습니다. ‘수정본 평가하기’는 내용을 변경하지 않고 독립 평가합니다. 섹션의 ‘검토함’은 읽기 기록이며 전체 승인이 아닙니다. 새로 승인하려면 현재 명세 기준의 평가에서 목표를 달성해야 합니다. 기존 승인본은 새 버전이 생겨도 자동 교체되지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 실행 중 중단되더라도 독립 평가가 끝난 정상 계획이 있다면 최신 명세·코드 일치를 확인한 뒤 저장합니다. 미완성 응답은 계획으로 저장하지 않습니다. 브라우저를 새로 열면 서버에 저장된 진행 기록을 불러옵니다. 서버 재시작은 실행을 중단하며 자동으로 AI를 재호출하지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 사용자가 계획을 적용할 수 있도록 파일 경로와 변경 종류, 적용에 필요한 전체 코드를 작성합니다. 길이를 이유로 코드나 import·설정·마이그레이션·테스트를 생략하지 않습니다. 출력 한도를 초과하면 명세를 나누어 다시 요청합니다. 언어를 지정한 코드 블록은 HTML 안에서 구문 색상으로 표시되며 다운로드에도 유지됩니다. ",
        )
      }}
    </p>
    <h3>{{ __t("계획서의 기본 구성") }}</h3>
    <table>
      <thead>
        <tr>
          <th>{{ __t("영역") }}</th>
          <th>{{ __t("기록할 내용") }}</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>{{ __t("요구사항 탭") }}</td>
          <td>
            {{
              __t(
                " 식별번호와 제목으로 구분하며, HTML 파일을 따로 열어도 스크립트 없이 탭을 전환합니다. ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("구현 방법") }}</td>
          <td>
            {{
              __t(
                " 변경할 파일·클래스·메서드, 처리 순서, 인터페이스, 대안과 선택 이유, 의존 관계와 미결정 사항. ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>BEFORE / AFTER</td>
          <td>
            {{
              __t(
                " 기존 코드와 수정 제안을 언어별 코드 블록으로 비교합니다. 실제 코드를 전달받지 않았다면 BEFORE는 ‘기존 코드 미제공’, AFTER는 ‘제안 예시’로 표시해야 합니다. ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("UI 목업") }}</td>
          <td>
            {{
              __t(
                " HTML/CSS로 화면과 상태를 표현합니다. 사용자 흐름, 빈 상태·오류·로딩을 포함하고 UI가 없는 경우 이유를 기록합니다. ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("DB 변경") }}</td>
          <td>
            {{
              __t(
                " DB 종류와 버전 확인, SQL·마이그레이션·인덱스·트랜잭션·롤백 및 기존 데이터 영향. 미확인 방언은 가정으로 표시합니다. ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("검증") }}</td>
          <td>
            {{
              __t(
                " 완료 기준 ID별 정상·경계·오류·동시성 시나리오, 명령, 기대 결과와 수동 확인. 미실행 테스트를 통과로 표시하지 않습니다. ",
              )
            }}
          </td>
        </tr>
      </tbody>
    </table>
    <p>
      {{
        __t(
          " 기본 스킬은 내용 작성법과 UI 규칙을 전달합니다. 서버가 탭·다섯 섹션·번호·색상·여백·BEFORE/AFTER 두 패널을 직접 구성하므로 생성마다 다른 전체 디자인이 만들어지지 않습니다. 화면이 좁으면 비교 패널을 세로로 배치합니다. UI 목업의 HTML/CSS는 별도 프레임에 격리되어 계획서 디자인을 바꾸지 않습니다. 설명·코드·SQL의 정확성은 사용자가 검토해야 합니다. 현재 코드 메뉴와 IDE 연결은 없으므로 보관·가져오기 된 코드가 없다면 실제 프로젝트를 읽지 못합니다. 이미지 첨부는 제목과 메타데이터만 전달하며 픽셀을 분석했다고 표현하지 않습니다. 기존 텍스트 계획과 구현 순서는 ‘이전 계획 기록’에서 보존합니다. ",
        )
      }}
    </p>
    <h3>{{ __t("서버 저장과 충돌 처리") }}</h3>
    <p>
      {{ __t(" HTML과 작성자·작성 시각·부모 버전·생성 모델·기준 커밋은 ")
      }}<code>spec.plans.versions</code
      >{{
        __t(
          "에 보관합니다. 파일 모드에서는 database.json, DB 연결 시에는 연결된 저장소가 원본입니다. 서버 데이터 폴더의 ",
        )
      }}<code>{{ __t("plans/워크스페이스ID/버전ID.html") }}</code
      >{{
        __t(
          "에도 HTML 파일을 저장하며 정적 공개 경로로 제공하지 않습니다. 별도 파일 다운로드 없이 새 탭의 검토 화면에서 열고 편집합니다. 워크스페이스 JSON 공유 파일과 HTML 문서에도 계획 버전과 최종 선택이 포함됩니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 저장과 최종 선택은 워크스페이스 커밋에도 기록됩니다. 기존 계획 버전을 수정하거나 덮어쓰는 대신 새 버전만 추가합니다. 계획당 최대 200,000자, 명세당 최대 50개 버전이며 전체 문서 한도는 10MB입니다. AI 요청 중 다른 사람이 프로젝트를 변경하면 저장을 거절합니다. 최신 내용을 확인해 다시 요청하세요. 중지하거나 형식이 잘못된 응답은 새 버전으로 저장하지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " HTML 미리보기는 격리된 프레임에서 열며 JavaScript·외부 리소스·폼 실행을 허용하지 않습니다. 다운로드한 HTML에도 실행 차단 정책을 포함합니다. 목업은 읽고 비교하는 화면이며 실제 애플리케이션 실행 환경이 아닙니다. ",
        )
      }}
    </p>
    <h3>{{ __t("메뉴와 모달 조작") }}</h3>
    <p>
      {{
        __t(
          " CodeWith 로고는 메뉴와 분리되어 있으며 홈 목록으로 이동합니다. 좌측 메뉴 경계 가운데의 손잡이로 접고 펼칠 수 있습니다. 모달은 Esc·바깥 클릭·닫기 버튼으로 닫습니다. 저장하지 않은 입력이 있으면 닫지 않고 계속 작성하거나 명시적으로 변경 내용을 버릴 수 있도록 안내합니다. 저장 중에는 닫기를 차단합니다. 편집기 블록 메뉴가 열려 있을 때 첫 Esc는 블록 메뉴를 닫습니다. ",
        )
      }}
    </p>
    <h3 id="project-source">{{ __t("프로젝트 연결과 자동 코드 읽기") }}</h3>
    <p>
      {{ __t(" 왼쪽 사용자 이름의 ")
      }}<b>{{ __t("개인 설정 → 프로젝트 연결 → 폴더 선택") }}</b
      >{{
        __t(
          "에서 프로젝트 경로를 입력하거나 탐색해 연결합니다. 이 폴더를 코드 읽기와 일반 AI 채팅의 파일 수정·명령 실행에 함께 사용합니다. 연결 정보는 현재 워크스페이스에서 나만 사용하며 다른 팀원의 연결을 바꾸지 않습니다. 서버에서 접근할 수 없는 폴더는 ",
        )
      }}<b>{{ __t("폴더 사본 가져오기") }}</b
      >{{ __t("로 분석용 코드를 가져올 수 있습니다. ") }}
    </p>
    <p>
      <b>{{ __t("폴더 접근 권한이 유지되면") }}</b
      >{{
        __t(
          " 질문하거나 AI 계획서를 만들 때 저장된 파일을 다시 읽습니다. 계획 작성 중 파일이 바뀌면 해당 결과의 저장을 거절합니다. 계획 작성은 실제 프로젝트 파일을 읽기만 합니다. 일반 채팅에서 실제 경로를 연결한 경우에는 승인 절차를 거쳐 파일 수정·명령 실행을 할 수 있습니다. IDE에서 저장하지 않은 편집 내용은 읽지 못합니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 지원 브라우저에서는 읽기 권한을 받은 폴더 핸들을 IndexedDB에 보관하고 AI 요청 전에 다시 읽습니다. 재접속 후 권한이 없으면 연결 상태를 ",
        )
      }}<b>{{ __t("재연결 필요") }}</b
      >{{ __t("로 표시합니다. ") }}<b>{{ __t("이전 폴더 재연결") }}</b
      >{{
        __t(
          " 버튼으로 저장된 폴더의 읽기 권한을 다시 허용할 수 있습니다. 폴더 접근 정보가 사라진 경우에는 기본 폴더 선택 창에서 다시 선택합니다. 지원되지 않는 환경에서는 폴더 업로드로 대체하며 ",
        )
      }}<b>{{ __t("사본 연결됨") }}</b
      >{{
        __t(
          "으로 표시합니다. 이 경우 선택한 시점의 사본만 보관하므로 코드 변경 후 다시 선택해야 합니다. 브라우저의 ",
        )
      }}<a
        href="https://developer.chrome.com/docs/capabilities/web-apis/file-system-access"
        >{{ __t("폴더 접근과 권한") }}</a
      >,
      <a
        href="https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/webkitdirectory"
        >{{ __t("폴더 업로드") }}</a
      >{{ __t(" 동작을 따릅니다. ") }}
    </p>
    <p>
      {{
        __t(
          " 서버에는 사용자별 연결 위치와 분석용 코드 사본을 보관합니다. 다른 멤버의 연결 위치·원본 사본은 공유 API나 워크스페이스 내보내기에 포함하지 않습니다. 사용자의 AI에 전달한 코드가 계획서 본문에 들어가면 그 본문은 팀과 공유되며, 계획의 분석 파일 경로·해시·시점도 계획 버전에 기록합니다. 연결 해제는 개인 연결과 사본만 삭제하며 원본 코드와 기존 계획서는 유지합니다. 이 브라우저에는 사용자·워크스페이스별로 마지막 폴더 정보를 남겨 재연결할 수 있습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 기본 제외 항목은 숨김 파일·폴더, node_modules·target·build·dist 등 의존성과 빌드 결과, 인증 관련 파일명입니다. 코드·설정·문서의 UTF-8 텍스트만 읽으며 바이너리·심볼릭 링크·200 KB 초과 파일은 제외합니다. 이름만으로 모든 비밀정보를 판별할 수는 없습니다. 민감한 설정은 ",
        )
      }}<b>{{ __t("제외할 상대 경로") }}</b
      >{{
        __t(
          "에 파일이나 폴더를 한 줄에 하나씩 입력하세요. 폴더 경로는 하위 전체에 적용됩니다. 이 입력은 와일드카드나 .gitignore 문법이 아니며, 저장소의 .gitignore를 자동 해석하지 않습니다. 한 연결은 최대 500개·2 MB입니다. 한도를 넘으면 범위를 좁혀야 하며 기존 연결을 덮어쓰지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " AI에는 질문과 요구사항에 관련된 파일을 경로·본문 키워드로 우선 선택해 약 100,000자 이내로 전달합니다. 파일을 중간에서 잘라 전달하지 않습니다. 계획 탭에는 전달된 파일 수, 전체 읽은 파일 수, 기준 해시와 실제 전달 파일 목록이 표시됩니다. 생략 파일은 미분석으로 모델에 알립니다. 정적 파일 문맥을 전달하는 단계이며 IDE의 전체 호출 그래프 분석이나 모델이 도구로 추가 파일을 조회하는 기능은 아직 없습니다. 코드가 달라지면 계획에 재검토를 표시합니다. 경로 연결은 최종 선택 전에도 다시 읽으며 브라우저 업로드 사본은 마지막 선택 시점을 기준으로 비교합니다. 다른 팀원의 폴더와 비교하지 못한 계획은 일치 여부 미확인으로 표시합니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 프로젝트 연결은 개인 설정으로 저장하여 워크스페이스 커밋을 만들지 않습니다. 계획서 저장은 기존대로 커밋과 계획 버전을 생성하고, 분석 기준은 ",
        )
      }}<code>codeSource</code
      >{{
        __t(
          "에 포함합니다. 이 방식으로 프로젝트 코드와 명세 파일을 섞거나 실제 소스에 계획 내용을 덮어쓰지 않습니다. ",
        )
      }}
    </p>
    <h3>{{ __t("선택 메뉴와 파일 입력") }}</h3>
    <p>
      {{
        __t(
          " 테마·모델·추론 강도·설정의 선택 목록은 공통 메뉴를 사용합니다. 방향키로 이동하고 Enter로 선택하며 Esc는 선택 목록만 닫습니다. Tab은 다음 입력으로 이동하고, 항목 이름의 첫 글자를 입력해 찾을 수 있습니다. 선택하지 않고 밖을 누르면 기존 값이 유지됩니다. 모달 안에서도 동일하게 동작합니다. 파일 선택 버튼·입력 테두리·포커스 표시도 현재 테마에 맞춰 표시하며 실제 파일 탐색 창은 운영체제의 기본 선택 창을 사용합니다. ",
        )
      }}
    </p>
    <h3>{{ __t("화면 테마") }}</h3>
    <p>
      {{
        __t(
          " 첫 화면과 상단의 화면 테마 선택에서 시스템 설정·라이트·다크를 고를 수 있습니다. 기본값인 시스템 설정은 운영체제의 색상 모드를 따르며 변경 시 즉시 적용됩니다. 직접 선택한 값은 이 브라우저에 저장하여 새로고침·새 탭에서도 유지합니다. 시스템 설정을 다시 선택하면 직접 지정한 값을 해제합니다. 테마를 변경해도 작성 중인 입력 내용은 유지됩니다. 안내서·로그인 대기 화면과 기본 계획서 미리보기에도 적용하며, 저장된 계획서 원본과 내려받은 HTML은 덮어쓰지 않습니다. ",
        )
      }}
    </p>
    <h3>{{ __t("새로고침과 로그인 유지") }}</h3>
    <p>
      {{
        __t(
          " PERSONAL 계정으로 들어간 뒤에는 서버 로그인 세션으로 접속 상태를 확인합니다. 새로고침하거나 같은 주소를 다시 열면 워크스페이스·명세·요구사항·계획·이력 화면을 현재 URL 그대로 유지합니다. 처음 접속했거나 세션이 만료되면 소개 화면에서 PERSONAL 계정을 누르세요. 이때도 요청한 주소는 보존하며 진입 후 해당 화면으로 이동합니다. ",
        )
      }}
    </p>
    <h3>{{ __t("명세 정보와 삭제") }}</h3>
    <p>
      {{
        __t(
          " 명세 상단에는 식별번호와 이름을 표시합니다. 내부 수정 버전은 계획의 변경 여부와 검증 대상을 구분하기 위한 값이며, 계획서 버전과 혼동하지 않도록 제목에서는 표시하지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 명세 제목 옆의 삭제 버튼을 누르면 식별번호·이름·요구사항 수·계획서 버전 수가 표시되는 확인 창이 열립니다. 확인 후 해당 명세와 포함된 요구사항·자료·계획을 삭제하고 전체 요구사항 화면으로 이동합니다. 마지막 명세도 삭제할 수 있습니다. 편집 권한이 필요하며, 다른 사람이 먼저 변경했다면 삭제를 거절합니다. 삭제는 워크스페이스 커밋으로 기록되어 이력에서 삭제 전 시점으로 복원할 수 있습니다. 이때 복원은 개별 명세만이 아니라 워크스페이스 전체에 적용됩니다. ",
        )
      }}
    </p>
    <h3 id="requirement-status">{{ __t("요구사항 상태와 자유로운 편집") }}</h3>
    <p>
      {{ __t(" 요구사항의 상태는 ") }}<code>pending</code
      >{{ __t("(구현 이전)과 ") }}<code>completed</code
      >{{
        __t(
          "(완료) 두 가지입니다. 명세 전체는 요구사항이 하나 이상 있고 모두 완료일 때만 완료로 표시합니다. 좌측 항목은 상태 아이콘 → 식별번호 → 이름 순서이며, 빨간 원은 구현 이전, 초록 체크 원은 완료입니다. 상태 텍스트는 좌측에 반복하지 않으며 마우스를 올리거나 스크린리더로 확인할 수 있습니다. 선택한 명세 아래의 요구사항을 누르면 해당 본문으로 이동합니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 생성·편집·삭제는 기존 확정·합의·검증 상태나 열린 질문에 의해 제한되지 않습니다. 완료 기준이 아직 없는 요구사항을 저장하거나 마지막 요구사항을 삭제할 수도 있습니다. 편집 권한과 중복 식별번호·잘못된 입력에 대한 검사는 유지합니다. 완료된 요구사항도 수정·삭제할 수 있으며, 식별번호·제목·본문·완료 기준·자료가 바뀌면 해당 요구사항의 version이 증가하고 상태는 구현 이전으로 돌아갑니다. 다른 요구사항의 완료 상태는 유지합니다. 복원된 요구사항은 현재 구현과 다시 대조할 수 있도록 구현 이전으로 돌아갑니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 완료 상태는 요구사항 카드에서 검증 요약과 함께 기록합니다. 작성자·시각·요구사항 버전·계획 버전·코드 리비전을 이력에 남기며, 요구사항 변경 시 이전 완료 상태를 초기화합니다.",
        )
      }}
    </p>
    <h2 id="completion">{{ __t("독립 앱에서 구현 완료 기록") }}</h2>
    <p>
      {{
        __t(
          "최종 계획을 승인한 뒤 선택한 개발 환경에서 구현하고 검증합니다. 요구사항 카드의 구현 완료 기록에서 검증 요약과 코드 리비전을 남깁니다. 별도 플러그인이나 로컬 브리지 설치는 필요하지 않습니다.",
        )
      }}
    </p>
    <p>
      {{
        __t(
          "완료 기록은 사용자의 검증 보고입니다. 서버가 프로젝트 테스트를 대신 실행했다는 뜻은 아닙니다. 요구사항이나 최종 계획이 변경되면 이전 버전의 완료 요청은 거절됩니다.",
        )
      }}
    </p>
    <h2 id="history">{{ __t("4. 프로젝트 전체의 변경 이력") }}</h2>
    <p>
      {{
        __t(
          " 각 저장은 작성자·시각·메시지·상위 커밋·변경 경로·프로젝트 스냅샷을 기록합니다. 코드와 명세가 함께 저장되므로 “이 코드가 어떤 요구사항을 기준으로 작성되었나”를 같은 이력에서 볼 수 있습니다. 두 사용자가 같은 커밋을 편집하다 한 사람이 먼저 저장하면 뒤의 저장은 409 충돌로 거절합니다. 편집 창은 유지되므로 작성한 내용을 복사해 두고 최신 내용을 불러와 다시 반영할 수 있습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 변경 이력에서 과거 커밋을 열고 복원하면 그 시점의 프로젝트 기준·모든 명세·첨부 자료·코드 파일을 복원하는 ",
        )
      }}<b>{{ __t("새 커밋") }}</b
      >{{
        __t(
          "이 생깁니다. 기존 이력을 삭제하거나 계정·멤버·LLM 로그인까지 되돌리지 않습니다. 이것은 CodeWith 내부의 선형 커밋 이력이며 Git 저장소, 브랜치, GitHub push를 구현한 것은 아닙니다. 이력은 서버에 저장되며 JSON 공유 파일과 HTML 문서에는 현재 문서만 담습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " AI 연결은 서비스 선택 → 연결 방식 선택 → 인증 → 기본 모델 자동 선택 및 연결 완료 순서입니다. 대화창의 연결 설정 버튼에서 시작합니다. 연결 후 같은 버튼이 연결됨으로 바뀌며 다시 누르면 연결을 관리할 수 있습니다. 상단 말풍선 아이콘으로 대화창을 열고 닫습니다. ",
        )
      }}
    </p>
    <h2 id="ai">{{ __t("5. AI 연결") }}</h2>
    <p>
      {{
        __t(
          " 현재 연결 해제를 누르면 버튼이 ‘연결 해제 중…’으로 바뀝니다. 처리 중에는 중복 요청을 막고, 완료하면 모달 안에 완료 메시지를 표시하며 채팅의 연결 상태·모델 목록도 초기화합니다. 실패하면 모달에 오류와 다시 시도 버튼이 표시됩니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 서비스 선택 → 구독 또는 API 선택 → 공식 인증 → 기본 모델 자동 선택 순서입니다. 개인 PC 연결 프로그램, 주소 입력, 연결 코드 입력은 필요하지 않습니다. 구독 연결의 공식 CLI는 CodeWith 서버에 설치합니다. 원격 서버의 Codex는 공식 장치 코드 인증, Claude는 필요하면 공식 인증 코드 입력을 사용합니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 웹 AI는 요구사항과 계획 작성을 돕습니다. 모델·effort와 대화는 서버에 개인별로 저장합니다. 실제 IDE 코드 작성과 테스트 실행은 아직 제공하지 않습니다. ",
        )
      }}
    </p>
    <p>
      <a href="/guide/ai">{{ __t("상세 AI 연결 안내") }}</a>
    </p>
    <h3>{{ __t("워크스페이스 HTML 문서 다운로드") }}</h3>
    <p>
      {{ __t(" 좌측 ") }}<b>{{ __t("기본 설정") }}</b
      >{{ __t("의 ") }}<b>{{ __t("워크스페이스 문서 → HTML 다운로드") }}</b
      >{{
        __t(
          "에서 현재 문서를 읽기용 HTML로 저장합니다. JSON 공유 파일과 템플릿은 상단 공유 아이콘에서 관리합니다. ",
        )
      }}
    </p>
    <h2 id="format">{{ __t("6. JSON 템플릿으로 직접 작성하고 불러오기") }}</h2>
    <p>
      {{ __t(" 화면 상단 ") }}<b>{{ __t("공유 파일") }}</b
      >{{
        __t(
          "에서 작성용 JSON 템플릿을 내려받거나 JSON을 직접 붙여넣을 수 있습니다. 빈 문서와 기본 예제는 소스의 ",
        )
      }}<code>templates/empty.json</code>, <code>templates/workspace.json</code
      >{{ __t("에도 있습니다. ")
      }}<a
        href="../templates/empty.json"
        download
        >{{ __t("빈 템플릿") }}</a
      >
      ·
      <a
        href="../templates/workspace.json"
        download
        >{{ __t("참가 등록 예제") }}</a
      >
    </p>
    <p>
      {{
        __t(
          " 기준 형식은 JSON v2입니다. YAML은 아직 지원하지 않습니다. HTML 문서로 내보낼 때도 같은 JSON을 ",
        )
      }}<code>codewith-data</code
      >{{
        __t(
          " 데이터 블록으로 넣습니다. 이 HTML은 다른 사람에게 전달해 오프라인으로 읽을 수 있고, CodeWith에 다시 불러와 편집할 수 있습니다. HTML의 보이는 글만 수정하면 내장 데이터가 바뀌지 않습니다. 프로그램으로 편집할 때는 JSON 데이터를 수정하세요. ",
        )
      }}
    </p>
    <pre>{{
      __t(
        ' { "format": "codewith.exchange", "version": 2, "document": { "schema": "codewith.spec.v2", "project": "참가 등록 프로젝트", "projectSpec": { "purpose": "중복 없이 참가를 등록한다.", "principles": ["행동 계약과 완료 기준으로 검증한다."], "constraints": ["Java 21", "IntelliJ IDEA"] }, "specs": [{ "id": "SPEC-REG", "title": "참가 등록", "subtitle": "이메일 중복 등록을 방지한다.", "version": 1, "status": "draft", "decisions": "중복 검사와 저장을 원자적으로 처리한다.", "codeScope": "RegistrationService와 참가 저장소", "requirements": [{ "id": "REG-001", "title": "중복 등록 방지", "body": "동일 이메일로 다시 요청하면 기존 등록을 유지하고 중복 오류를 반환한다.", "status": "draft", "criteria": [ {"id": "REG-001-AC1", "text": "첫 요청은 등록된다."}, {"id": "REG-001-AC2", "text": "두 번 요청해도 참가자는 한 명이다."} ], "resources": [] }], "tasks": [ {"id": "TASK-01", "text": "등록 서비스를 구현한다.", "req": "REG-001", "done": false} ], "questions": [], "evidence": [] }], "files": {} } }',
      )
    }}</pre>
    <table>
      <thead>
        <tr>
          <th>{{ __t("필드") }}</th>
          <th>{{ __t("작성 규칙") }}</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>format / version / document.schema</td>
          <td>{{ __t("예제의 값 유지. 교환 버전과 명세 버전은 별개") }}</td>
        </tr>
        <tr>
          <td>projectSpec</td>
          <td>
            {{
              __t(
                " purpose·instructions 문자열, principles·constraints 문자열 배열, skills 스킬 배열 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>specs</td>
          <td>{{ __t("개발 단위 0~100개. 서로 다른 명세 ID") }}</td>
        </tr>
        <tr>
          <td>status</td>
          <td>
            {{
              __t(
                " 명세: draft, review, accepted, implementing, verified. 요구사항: draft, accepted ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>requirements[].id</td>
          <td>
            {{
              __t(
                " 프로젝트 전체에서 유일한 사용자 지정 ID. 자동 생성값을 쓸 필요 없음 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>criteria</td>
          <td>
            {{
              __t(
                " 각각 id·text를 갖는 배열. 확정하려면 하나 이상의 비어 있지 않은 조건 필요 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>tasks[].req / evidence[].req</td>
          <td>
            {{
              __t(
                " 같은 명세에 존재하는 요구사항 ID 또는 빈 문자열. ID 변경 시 참조도 수정 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>questions</td>
          <td>
            {{
              __t(
                "{id, text, answer, resolved}. 결정 전 answer는 빈 문자열 가능",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>evidence</td>
          <td>
            {{
              __t(
                " {id, title, detail, code, req, at, specVersion, result}. result는 pending/pass/fail, at는 ISO 날짜, specVersion은 검증한 버전 숫자 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>files</td>
          <td>
            {{
              __t(
                " 프로젝트 상대 경로를 키로, 전체 텍스트 내용을 값으로 갖는 객체. 상위·절대·숨김 경로 불가 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>createdBy / updatedBy / updatedAt</td>
          <td>
            {{
              __t(
                " 직접 넣을 필요 없음. 불러와 저장할 때 서버가 실제 사용자로 기록하며 가져온 작성자 정보를 인증 정보로 신뢰하지 않음 ",
              )
            }}
          </td>
        </tr>
      </tbody>
    </table>
    <p>
      {{
        __t(
          " 새 프로젝트로 불러오면 현재 프로젝트를 보존합니다. 현재 프로젝트 전체 교체로 불러오면 모든 명세와 코드 파일을 입력 데이터로 바꾸고 새 커밋을 만듭니다. 삭제되는 파일도 있으므로 교체 범위를 확인하세요. 파일 수 최대 150개, 파일별 텍스트 200,000자, 프로젝트 JSON 전체 약 10 MB를 제한으로 검사합니다. 이미 서버 폴더에서 외부 편집된 파일은 자동 덮어쓰지 않습니다. ",
        )
      }}
    </p>
    <h2 id="materials">
      {{ __t("7. 요구사항에 목업·표·그래프·흐름도 넣기") }}
    </h2>
    <p>
      {{ __t(" 요구사항을 저장한 뒤 ")
      }}<b>{{ __t("참고 자료 → 자료 추가 · 편집") }}</b
      >{{
        __t(
          "을 열면 이미지, 표, 막대그래프, 흐름도, 설명을 만들 수 있습니다. 각각 ID와 이름을 직접 정할 수 있습니다. 자료는 요구사항 데이터에 포함되므로 JSON 공유, HTML 내보내기, 이력 복원에 함께 따라갑니다. ",
        )
      }}
    </p>
    <table>
      <thead>
        <tr>
          <th>{{ __t("유형") }}</th>
          <th>{{ __t("직접 작성 방법") }}</th>
          <th>{{ __t("제한") }}</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>{{ __t("이미지 · 목업") }}</td>
          <td>
            {{
              __t(
                " 목업·차트·디자인을 PNG/JPEG/WebP/GIF로 업로드. JSON에는 data URL로 내장 ",
              )
            }}
          </td>
          <td>
            {{
              __t(
                " 파일당 1 MB, 자료당 이미지 1개. SVG/HTML 실행 파일은 업로드하지 않음 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("표") }}</td>
          <td>
            {{
              __t(
                " 첫 줄에 열 이름, 탭으로 열 구분. 스프레드시트 복사·붙여넣기 가능 ",
              )
            }}
          </td>
          <td>{{ __t("20열·100행, 모든 행의 열 수 동일") }}</td>
        </tr>
        <tr>
          <td>{{ __t("막대그래프") }}</td>
          <td>{{ __t("한 줄마다 이름, 숫자 입력. 단위를 이름에 명시") }}</td>
          <td>
            {{
              __t(
                " 0 이상 숫자, 최대 50개. 자동 측정 결과가 아니라 입력 데이터의 시각화 ",
              )
            }}
          </td>
        </tr>
        <tr>
          <td>{{ __t("흐름도") }}</td>
          <td>{{ __t("수직으로 이어지는 단계를 독립 목록으로 입력") }}</td>
          <td>{{ __t("1~50단계. 임의 분기·자유 배치 편집기는 아직 없음") }}</td>
        </tr>
        <tr>
          <td>{{ __t("설명 · 링크") }}</td>
          <td>{{ __t("자료 설명과 출처 URL을 텍스트로 기록") }}</td>
          <td>{{ __t("외부 페이지를 자동으로 읽거나 실행하지 않음") }}</td>
        </tr>
      </tbody>
    </table>
    <h3>{{ __t("JSON 자료 예제 · requirements[].resources 배열에 넣기") }}</h3>
    <pre>{{
      __t(
        ' [ { "id": "REF-TABLE", "type": "table", "title": "응답 계약", "headers": ["요청", "결과"], "rows": [["신규 이메일", "등록 성공"], ["중복 이메일", "중복 오류"]] }, { "id": "REF-CHART", "type": "chart", "title": "응답 시간(ms)", "values": [{"label": "목표", "value": 200}, {"label": "측정값", "value": 160}] }, { "id": "REF-FLOW", "type": "flow", "title": "등록 흐름", "steps": ["이메일 입력", "입력 정규화", "중복 확인과 저장", "결과 반환"] }, { "id": "REF-NOTE", "type": "note", "title": "디자인 설명", "text": "등록 성공 시 확인 메시지를 표시한다. 디자인 원본: https://example.com/mockup" } ]',
      )
    }}</pre>
    <p>
      {{ __t(" 이미지는 ")
      }}<code>{{
        __t(
          '{"id":"REF-IMAGE","type":"image","title":"등록 화면","data":"data:image/png;base64,..."}',
        )
      }}</code
      >{{
        __t(
          " 구조입니다. 실제 base64 이미지 값이 필요하므로 UI에서 업로드한 뒤 JSON을 내보내는 것이 편리합니다. 현재 AI 요청은 텍스트 기반이므로 이미지 자료의 픽셀을 시각적으로 분석하지 않습니다. 표·그래프·흐름도의 구조화 데이터와 자료 이름은 문맥에 포함할 수 있지만, 디자인 이미지 검토가 필요하면 핵심 내용을 텍스트로도 설명하세요. ",
        )
      }}
    </p>
    <h2 id="storage">
      {{ __t("8. 사용자가 선택하는 DB 연결 · 미설정 시 파일 저장") }}
    </h2>
    <p>
      {{
        __t(
          " DB 종류는 사용자가 정합니다. 서버는 공통 저장소 인터페이스를 사용하며 기본 어댑터로 SQLite, PostgreSQL, MySQL/MariaDB를 제공합니다. 다른 DB도 사용자 정의 어댑터로 연결할 수 있습니다. 아무 연결도 설정하지 않으면 기존 ",
        )
      }}<code>.codewith/database.json</code>{{ __t("에 저장합니다. ") }}
    </p>
    <p>
      <code>codewith/.env.example</code>{{ __t("을 ") }}<code>.env</code
      >{{ __t("로 복사해 연결 문자열을 설정한 뒤 ") }}<code>npm start</code
      >{{
        __t(
          "로 실행하세요. 환경변수로 직접 전달해도 됩니다. PostgreSQL·MySQL/MariaDB 드라이버는 ",
        )
      }}<code>npm install</code
      >{{
        __t(
          "로 설치됩니다. 파일과 SQLite 모드에는 외부 DB 서버가 필요하지 않습니다. ",
        )
      }}
    </p>
    <pre>{{
      __t(
        " # 파일 저장소 — 기본값 CODEWITH_DATABASE_URL= # 아래 예제 중 본인 DB 하나를 선택해서 설정 CODEWITH_DATABASE_URL=sqlite:codewith.sqlite CODEWITH_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/codewith CODEWITH_DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/codewith CODEWITH_DATABASE_URL=mariadb://USER:PASSWORD@HOST:3306/codewith",
      )
    }}</pre>
    <p>
      {{
        __t(
          " 예제의 USER, PASSWORD, HOST를 본인의 연결 정보로 바꾸세요. URL의 비밀번호에 특수문자가 있다면 URL 인코딩해야 합니다. 연결 문자열은 소스나 공유 JSON에 넣지 않습니다. SQLite 상대 경로는 ",
        )
      }}<code>CODEWITH_DATA_DIR</code>{{ __t(" 기준이며, 기본값에서는 ")
      }}<code>.codewith/codewith.sqlite</code
      >{{
        __t(
          "가 됩니다. 외부 DB는 DB 자체와 계정을 먼저 만들고 해당 DB 안에서 CodeWith 테이블을 생성·조회·수정할 수 있는 권한을 부여해야 합니다. 외부 DB의 TLS 옵션은 선택한 드라이버의 연결 설정에 따라 구성합니다. ",
        )
      }}
    </p>
    <div class="note">
      <b>{{ __t("연결 설정 없음") }}</b
      >{{ __t("과 ") }}<b>{{ __t("설정한 DB의 장애") }}</b
      >{{
        __t(
          "는 다릅니다. 설정이 없으면 파일 저장소를 사용합니다. 주소 오류·인증 실패·저장 실패가 발생하면 요청/시작을 중단합니다. 이때 몰래 파일 저장소로 전환하지 않으므로 저장 위치가 두 곳으로 갈라지지 않습니다. ",
        )
      }}
    </div>
    <h3>{{ __t("기존 파일 데이터를 DB로 가져오기") }}</h3>
    <pre>
CODEWITH_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/codewith
CODEWITH_IMPORT_FILE=1</pre>
    <p>
      {{ __t(" 처음 사용하는 비어 있는 CodeWith DB 상태에만 기존 ")
      }}<code>database.json</code
      >{{
        __t(
          "을 복사합니다. 이미 데이터가 들어 있는 DB는 덮어쓰지 않습니다. 원래 JSON 파일도 보존합니다. 가져온 뒤 ",
        )
      }}<code>CODEWITH_IMPORT_FILE</code
      >{{
        __t(
          " 설정을 지우세요. 명세뿐 아니라 사용자·암호 해시·멤버·커밋·개인 대화도 유지됩니다. 코드 파일과 아직 로컬로 이전하지 않은 구버전 AI 정보는 기존 데이터 폴더에 있으므로 ",
        )
      }}<b>{{ __t("같은 데이터 폴더를 계속 사용") }}</b
      >{{ __t("해야 합니다. ") }}
    </p>
    <p>
      {{
        __t(
          " DB를 사용한 이후 JSON 파일은 자동 동기화하지 않습니다. DB 설정을 지워 파일 모드로 돌아가면 그 JSON에 남아 있는 예전 데이터가 열립니다. 저장소 전환 전에 백업하고, 프로젝트 교환은 JSON/HTML 내보내기를 사용하세요. DB 전환은 코드 파일과 개인 PC의 AI 데이터를 이동하지 않습니다. ",
        )
      }}
    </p>
    <h3>{{ __t("다른 DB를 위한 어댑터") }}</h3>
    <pre>
CODEWITH_DATABASE_URL=vendor-specific-connection-string
CODEWITH_STORAGE_ADAPTER=/absolute/path/to/my-database-adapter.mjs</pre>
    <p>
      {{
        __t(
          " 서버 운영자가 신뢰하는 로컬 모듈 경로를 지정합니다. 이 모듈은 서버 코드로 실행되므로 일반 멤버가 웹 화면에서 임의 파일을 지정하는 기능으로 노출하지 않습니다. 다음 인터페이스를 구현하면 라우트나 명세 편집기를 바꾸지 않고 연결할 수 있습니다. ",
        )
      }}
    </p>
    <pre>{{
      __t(
        " // my-database-adapter.mjs — 아래는 구현 계약이며 완성된 드라이버는 아닙니다. export async function open({ dataDir, databaseUrl }) { // 원하는 DB 드라이버로 연결하고 초기화한다. return { kind: 'my-database', async read() { /* 저장된 CodeWith 상태 객체를 반환한다. */ }, async write(state) { /* 원자적 저장 완료 후 resolve, 실패하면 reject한다. */ }, async close() { /* 진행 중 저장을 마치고 연결을 닫는다. */ } }; }",
      )
    }}</pre>
    <p>
      {{ __t(" 상태 객체는 ")
      }}<code>{version:2, users:[], sessions:[], workspaces:[], chats:[]}</code
      >{{
        __t(
          "입니다. 어댑터는 빈 DB 초기화, 순서가 있는 저장, 동시 작성 충돌, 재시작 복원, 오류 처리를 책임집니다. 현재 기본 SQL 어댑터는 ",
        )
      }}<code>codewith_state</code
      >{{
        __t(
          " 테이블에 상태 JSON과 revision을 원자적으로 기록하는 방식입니다. 아직 사용자·명세별로 정규화한 관계형 스키마는 아니며, 단일 앱 프로세스 기준입니다. revision 불일치가 있으면 다른 서버의 변경을 덮어쓰지 않고 중단합니다. ",
        )
      }}
    </p>
    <p>
      {{ __t(" 드라이버 구현 근거: ")
      }}<a href="https://nodejs.org/api/sqlite.html">Node.js SQLite</a>,
      <a href="https://node-postgres.com/features/connecting">{{
        __t("node-postgres 연결 문서")
      }}</a
      >,
      <a href="https://sidorares.github.io/node-mysql2/docs">{{
        __t("MySQL2 공식 문서")
      }}</a
      >.
    </p>
    <h2 id="operations">{{ __t("9. 저장·운영과 현재 구현 범위") }}</h2>
    <p>
      {{ __t(" 파일 모드의 기본 데이터 폴더는 ")
      }}<code>codewith/.codewith</code>{{ __t("입니다. ")
      }}<code>CODEWITH_DATA_DIR</code
      >{{ __t("로 영속 폴더를 지정할 수 있습니다. ") }}<code>database.json</code
      >{{
        __t(
          "에는 계정·프로젝트·커밋을 저장합니다. 기존 코드 파일은 projects/워크스페이스ID에 보존합니다. 새 AI 인증·대화·모델 설정은 서버에 개인별로 보관합니다. API 키 암호화 키 ai-vault.key와 공식 CLI 인증 폴더 ai도 함께 백업해야 합니다. 구버전 데이터는 자동 삭제하지 않습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 전체 서버를 옮길 때는 서버를 중지한 뒤 데이터 폴더 전체를 안전하게 백업·복원하세요. 프로젝트 데이터 교환은 JSON 공유 파일을, 읽기용 문서 저장은 기본 설정의 HTML 다운로드를 사용하세요. 데이터 폴더는 Git과 정적 파일 제공에서 제외되어 있습니다. 그러나 OS 파일 권한과 디스크 암호화까지 이 앱이 대신 관리하지는 않습니다. ",
        )
      }}
    </p>
    <p>
      <b>{{
        __t(
          "현재는 단일 Node 프로세스 기준이며 파일 또는 선택한 DB 저장소를 사용합니다.",
        )
      }}</b
      >{{
        __t(
          " 같은 데이터 폴더를 여러 서버 프로세스나 클러스터가 동시에 쓰지 마세요. 이 버전은 소규모 팀에 맞춘 구현이며 대규모 운영에는 정규화된 DB 스키마, 여러 앱 인스턴스 간 동시성 제어, 백업 정책, 인증 운영 기능을 추가해야 합니다. 문서 편집은 즉시 공동 타이핑 방식이 아니라 저장 단위이며 최신 변경은 편집 창을 열지 않았을 때 주기적으로 확인합니다. CRDT 동시 편집, 브랜치 병합, SSO, 메일 초대, 비밀번호 복구는 아직 없습니다. ",
        )
      }}
    </p>
    <p>
      {{
        __t(
          " 참고 코드 메뉴는 제거했습니다. 기존 문서의 코드 데이터와 이력은 보존하지만 IDE 코드 편집·테스트 실행과 양방향 동기화는 아직 구현하지 않았습니다. ",
        )
      }}
    </p>
    <h3>{{ __t("검증 명령") }}</h3>
    <pre>{{
      __t(
        " npm test npm run check # 로컬 Chromium 설치 경로를 지정할 수 있는 실제 브라우저 검증 # CODING_CHROMIUM=/path/to/chromium npm run test:browser npm run test:browser",
      )
    }}</pre>
    <p>
      {{
        __t(
          " 통합·브라우저 테스트의 AI는 주입된 테스트 어댑터로 동작하여 계정 분리, 옵션 전달, 응답 반영과 오류를 재현합니다. 실제 유료 모델의 품질이나 계정 로그인 완료를 대신 검증하지 않습니다. 사용자 자신의 계정으로 연결한 뒤 실제 응답을 확인하세요. ",
        )
      }}
    </p>
    <p>
      {{ __t(" 화면은 한국어로 구성하며 안정적인 메뉴·상태·역할 키는 ")
      }}<code>src/services/i18n.js</code
      >{{
        __t(
          "에 분리했습니다. 문서 스키마는 언어와 독립적입니다. 영어 UI 전체 번역과 언어 전환은 후속 작업입니다. ",
        )
      }}
    </p>
    <p>
      <a href="http://localhost:4310">{{ __t("로컬 CodeWith 열기") }}</a>
    </p>
  </article>
</template>
