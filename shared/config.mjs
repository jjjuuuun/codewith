import runtimeDefaults from "../config/runtime.defaults.json" with { type: "json" };
// Browser/server contract defaults: change here and rebuild both sides together.
export const LIMITS = Object.freeze({
  textChars: 20000,
  shortTextChars: 3000,
  summaryChars: 5000,
  labelChars: 1000,
  messageChars: 16000,
  codeFileChars: 200000,
  documentChars: 10000000,
  requestBytes: 12000000,
  imageBytes: 1000000,
  imageDataChars: 1400000,
  attachmentCount: 5,
  attachmentBytes: 6 * 1024 * 1024,
  attachmentTextChars: 160000,
  exchangeBytes: 35000000,
  planImportBytes: 8000000,
  planHtmlChars: 2000000,
  planSourceChars: 200000,
  planSectionChars: 80000,
  planReasonChars: 4000,
  planExecutionChars: 2000000,
  chatContextChars: 100000,
  aiMinTokens: 1000,
  aiMaxTokens: 100000,
  planQuestionCount: 8,
  planQuestionChars: 1000,
});
export const AUTH_POLICY = Object.freeze({
  flowMs: 600000,
  keyChallengeMs: 120000,
  sessionSeconds: 604800,
  pendingFlows: 1000,
  keyFileBytes: 10000,
  defaultAttempts: 30,
  createAttempts: 15,
  passkeyAttempts: 60,
  emailAttempts: 8,
  maxKeys: 10,
});
export const AI_DEFAULTS = Object.freeze({
  provider: "codex",
  model: "",
  effort: "auto",
  summary: "auto",
  style: "balanced",
  serviceTier: "auto",
  webSearch: "auto",
});

export const PLAN_DEFAULTS = Object.freeze({
  mode: "review",
  loop: false,
  adaptive: false,
  discussion: false,
  discussionReviewers: 1,
  targetScore: 90,
  writers: 2,
  reviewers: 2,
  rounds: 1,
  maxCalls: 1,
  timeoutSeconds: runtimeDefaults.planTimeoutSeconds,
});
export const PLAN_POLICY = Object.freeze({
  writersMin: 2,
  writersMax: 4,
  reviewersMax: 3,
  discussionReviewersMax: 3,
  roundsMax: 5,
  callsMax: 64,
  defaultCallBudget: 24,
  timeoutMinSeconds: 30,
  timeoutMaxSeconds: 86400,
  versionsMax: 50,
  approvalsMax: 500,
  reviewIssuesMax: 30,
});
